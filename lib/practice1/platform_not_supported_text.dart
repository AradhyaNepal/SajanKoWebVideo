import 'package:flutter/material.dart';

class PlatformNotSupportedText extends StatelessWidget {
  const PlatformNotSupportedText({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text("Platform Not Supported");
  }
}
